let plus=S.g('ccPlus'),gates=0;
const _plusStart=startLevel;startLevel=function(n){let up=n&&state==='end'&&landWin&&zone===2;_plusStart(n);if(up)S.s('ccPlus',++plus);gates=0;if(n&&plus){let q=Math.min(plus,4);timeLeft=Math.max(70,timeLeft-q*4);addClean(Math.min(plus,3));for(let c of cars)c.v*=1+q*.08;lmText='STAMPEDE+'+(plus+1)+' • '+ZN[zone];lmTextT=2}};
const _plusPaint=paintStamp;paintStamp=(u,x=u.x,y=u.y,r=28)=>_plusPaint(u,x,y,r+Math.min(plus,3)*3);
const _plusZone=updateZone;updateZone=function(dt){_plusZone(dt);if(!level||!plus)return;if(zone!==2){windT-=dt;if(windT<=0){windT=4;wind*=-1;lmText=wind>0?'PRISM GUST →':'← PRISM GUST';lmTextT=1}for(let u of unis)if(u.live)u.vx+=wind*(28+plus*10)*dt}for(let i=0;i<6;i++)if(!(gates>>i&1)){let x=390+i*475,y=i%2?540:1130;for(let u of unis)if(u.live&&Math.hypot(u.x-x,u.y-y)<70){gates|=1<<i;u.boost=Math.max(u.boost,2.4);paintStamp(u,x,y,75);award('PRISM GATE',350);break}}};
const _plusWorld=world;world=function(){_plusWorld();if(!level||!plus)return;X.save();X.translate(ox,oy);X.scale(z,z);X.lineCap='round';for(let i=0;i<6;i++)if(!(gates>>i&1)){let x=390+i*475,y=i%2?540:1130;for(let q=0;q<3;q++){X.strokeStyle=`hsla(${clock*45+i*53+q*90} 95% 65%/.8)`;X.lineWidth=9-q*2;X.beginPath();X.arc(x,y,54-q*10,Math.PI,T);X.stroke()}X.strokeStyle='#fff';X.lineWidth=4;X.beginPath();X.moveTo(x-54,y);X.lineTo(x-54,y+26);X.moveTo(x+54,y);X.lineTo(x+54,y+26);X.stroke()}X.restore()};
const _plusEnd=end;end=function(){_plusEnd();if(level&&zone===2&&landWin){X.fillStyle='#a8f3ff';text('STAMPEDE+ UNLOCKED • PRISM GATES + REMIX PRESSURE',W/2,548,14,'center')}};
